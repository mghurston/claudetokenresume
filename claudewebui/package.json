{
  "name": "claude-cli-studio",
  "version": "0.1.0",
  "private": true,
  "description": "A local, cross-platform web interface for Claude Code CLI sessions.",
  "type": "module",
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning start.mjs",
    "test": "node --no-warnings=ExperimentalWarning --test",
    "server": "node --no-warnings=ExperimentalWarning server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220"
  },
  "engines": {
    "node": ">=20.10"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
